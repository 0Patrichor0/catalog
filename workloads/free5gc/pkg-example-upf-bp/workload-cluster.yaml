apiVersion: infra.nephio.org/v1alpha1
kind: WorkloadCluster
metadata:
  name: workload-cluster
  annotations:
    config.kubernetes.io/local-config: "true"
spec:
  clusterName: edge-1 # kpt-set: ${cluster-name}
  cnis:
  - macvlan
  - ipvlan
  - sriov
  masterInterface: eth1 # kpt-set: ${master-interface}

